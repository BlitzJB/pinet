// Browser stub for the Node crypto provider. The web app always injects
// `webCryptoProvider`, so this fallback is never used; it exists only so the
// bundler can resolve the shared client's Node fallback import.
export const nodeCryptoProvider = undefined as never;
